@using PureGreenLandGroup.Models.ViewModel.SiteControllerManagement
@using PureGreenLandGroup.Utility.Enums
@model List<ManufacturerViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Manufacturers List";
}
@{
    ManufacturerViewModel manufacturerVM = new();
}

<link href="https://cdn.datatables.net/1.10.20/css/jquery.dataTables.css" rel="stylesheet" type="text/css" />

<div class="rightSection">
    <div class="row">
        <div class="row mb-2">
            <div class="col-auto">
                <a class="btn btn-secondary-custom" asp-action="AdminPanel" asp-controller="MasterDataManager">Back to statistics </a>
            </div>
            <div class="col-auto ms-auto">
                <a class="btn btn-success col-auto" onclick="OpenMasterDataPopup()"><i class="fas fa-plus"></i> New</a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-bordered dataTable no-footer table-striped" id="ManufacturersTable">
                <thead class="bg-dark text-white">
                    <tr>
                        <th>Manufacturer Name</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Count > 0)
                    {
                        int counter = 1;
                        foreach (var manufacturer in Model)
                        {
                            <tr>
                                <td>@manufacturer.ManufacturerName</td>
                                <td>
                                    @if (manufacturer.IsActive)
                                    {
                                        <div style="color:#3ea304;">
                                            Active
                                        </div>
                                    }
                                    else
                                    {
                                        <div style="color:gray;">
                                            Inactive
                                        </div>
                                    }
                                </td>
                                <td>
                                    @{
                                        var requestURl = "/MasterDataManager/GetManufacturer?id=" + @manufacturer.Id;
                                    }
                                    <a class="text-success editBtn" onclick="PopulateMasterEntityDetail('@requestURl','manufacturerVM_Id','manufacturerVM_ManufacturerName')">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    @if (manufacturer.IsActive)
                                    {
                                        <a class="text-warning ">
                                            <label class="switch">
                                                <input type="checkbox" checked onclick="ChangeMasterDataStatusConfirmation('@manufacturer.Id','false','@MasterEntityType.manufacturer')">
                                                <span class="slider round"></span>
                                            </label>
                                        </a>
                                    }
                                    else
                                    {
                                        <a class=" text-warning">
                                            <label class="switch">
                                                <input type="checkbox" onclick="ChangeMasterDataStatusConfirmation('@manufacturer.Id','true','@MasterEntityType.manufacturer')">
                                                <span class="slider round"></span>
                                            </label>
                                        </a>
                                    }
                                </td>
                            </tr>
                            counter++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade modal-lg modal-lg-custom" id="CreateManufacturerPopup" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header d-flex justify-content-center">
                <h5 class="modal-title">Create New Manufacturer</h5>
                <button type="button" class="btn-close inspectionBtnClose " data-bs-dismiss="modal" aria-label="Close" onclick="CloseMasterDataPopups()"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <form method="post" asp-action="CreateManufacturer" asp-controller="MasterDataManager" enctype="multipart/form-data" id="ManufacturerForm">
                <div class="modal-body">
                    <div class="m-2">
                        <input type="hidden" asp-for="@manufacturerVM.Id" />
                        <div class="form-group ">
                            <label asp-for="@manufacturerVM.ManufacturerName" class="mb-2">Manufacturer Name<span class="text-danger fw-bold ">*</span></label>
                            <input type="text" asp-for="@manufacturerVM.ManufacturerName" placeholder="Enter manufacturer name.." class="form-control form-control-custom" maxlength="100" />
                            <span asp-validation-for="@manufacturerVM.ManufacturerName" class="text-danger validation-font" id="ManufacturerName_Validation"></span>
                        </div>
                    </div>
                </div>
                <div class="modal-footer d-flex justify-content-center mt-2">
                    <button type="submit" class="btn btn-success"> SUBMIT</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="~/AdminDashboard/js/jquery.min.js"></script>
<script src="~/admindashboard/datatable/datatable_puregreen.js"></script>
<script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
<script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
<script src="~/admindashboard/js/masterdatehandler.js"></script>
