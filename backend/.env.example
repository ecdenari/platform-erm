# Database Configuration
CONNECTION_STRING=Host=localhost;Database=platform_erm;Username=postgres;Password=postgres

# Development Database
DEV_CONNECTION_STRING=Host=localhost;Database=platform_erm_dev;Username=postgres;Password=postgres

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ISSUER=platform-erm
JWT_AUDIENCE=platform-erm-users
JWT_EXPIRY_MINUTES=60

# API Configuration
API_BASE_URL=http://localhost:5000
CORS_ORIGINS=http://localhost:5173,https://localhost:5173

# Logging
LOG_LEVEL=Information
LOG_FILE_PATH=logs/platform-erm.log

# External Services
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Development Settings
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000